# Data Directory

This directory separates local private data from public mock examples.

## Local Only

- `data/raw/`
- `data/processed/`

These directories are ignored by Git except for `.gitkeep`.

## Public Examples

`data/public_examples/` may contain synthetic mock examples only. Do not place real collected text here.
